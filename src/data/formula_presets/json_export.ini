[Preset]
Name=JSON Export
Description=Export all fields as JSON values
Separator=,
QuoteChar="
IncludeHeader=1

[Formulas]
STRING='"' || $1 || '"'
INTEGER=$1
FLOAT=$1
BOOLEAN=$1
DATE='"' || CAST($1 AS VARCHAR(10)) || '"'
TIMESTAMP='"' || CAST($1 AS VARCHAR(19)) || '"'
